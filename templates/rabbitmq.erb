  rabbitmq:
    name: <%= "#{@name}" %>
    host: <%= "#{@host}" %>
    port: <%= "#{@port}" %>
    verify_ssl_cert: <%= "#{@verify_ssl_cert}" %>
    username: <%= "#{@username}" %>
    password: <%= "#{@password}" %>
