  haproxy:
    name: <%= "#{@name}" %>
    scheme: <%= "#{@scheme}" %>
    host: <%= "#{@host}" %>
    port: <%= "#{@port}" %>
    verify_ssl_cert: <%= "#{@verify_ssl_cert}" %>
    path: <%= "#{@path}" %>
