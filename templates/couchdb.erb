  mongodb:
    name: <%= "#{@name}" %>
    host: <%= "#{@host}" %>
    port: <%= "#{@port}" %>
    username: <%= "#{@username}" %>
    password: <%= "#{@password}" %>
    verify_ssl_cert: <%= "#{@verify_ssl_cert}" %>
